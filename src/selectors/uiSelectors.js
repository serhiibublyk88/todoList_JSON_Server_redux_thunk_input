
export const selectSearchTerm = (state) => state.ui.searchTerm || "";
export const selectIsSorted = (state) => state.ui.isSorted || false;
export const selectIsProcessing = (state) => state.ui.isProcessing || false;
export const selectLoadingMessage = (state) => state.ui.loadingMessage || "";
